version: '3.9'

services:
  web:
    build: .
    container_name: django_backia
    volumes:
      - .:/app
      - ./db.sqlite3:/app/db.sqlite3  # opcional si quieres persistencia del archivo fuera del contenedor
    ports:
      - "8020:8000"
    env_file:
      - .env
